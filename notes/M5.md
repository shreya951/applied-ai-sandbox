# M5 — Task Decomposition

## Decomposition (8-10 tasks)
 1. Add tags to the note dictionary by putting it into a list, and verify by unit tests to confirm that the tags exist.
2. Be able to name each tag by adding an input area, verify by running code to ensure this functionality  works.
3. Create tags on each task by adding some CSS code, and verify by creating a test to run the cards and create tags on them. 
4. Check that there are no duplicates by putting them in a set, and verify by ensuring no errors arise when the code is run. 
5. Create the tests for each tag by generating a unit test with specific requirements and verify by ensuring it passes.
6. Create a filter to filter out the tags by adding some CSS code, and verify by running the program to ensure the functionality works.

## Task 1 context brief
Goal: Add a tags feature to the sandbox notes app.

Constraints:

* Each note can have zero or more tags stored as free-text strings.
* Tags should display as small chips next to each note title in the notes list view.
* Add a filter bar at the top with options like "All", "personal", "work", etc.
* Clicking a filter only shows notes containing that tag.
* Do not break existing functionality or tests.
* Prevent duplicate tags on the same note.

Files:

* Update the note model/data structure.
* Update the notes list template/UI.
* Update or add tests for tags and filtering behavior.

Acceptance:

* Existing tests still pass.
* New tests verify that:
  * tags are stored correctly
  * duplicate tags are removed
  * filtering by tag works
  * notes without matching tags are hidden
* Running the app shows visible tag chips and a working filter bar.

Plan mode first:
Before writing code, explain:

1. Which files you will change
2. How tags will be stored
3. How filtering will work
4. Which tests you will add
                

## Task 1 — what happened
- Claude's plan summary: Generally, Claude planned on changing a few specific lines of code that would allow for the functionalitites I was looking for.
- What I pushed back on: I didn't need to push back on anything specific, as overall, the steps Claude wanted to take were accurate.
- Verification result: All the tests passed. 


## Task 2 — what happened
- Since task 2 was mostly already implemented, I just needed to add some specifications.
- I pushed back on changing other tests to ensure that this code passed.
- All the tests passed.

## Task 3 — what happened
- I needed to be very specific with Claude for task 3, as it kept trying to run other functions.
- I pushed back on changing any code that was unnecessarily changed for the project.
- All tests passed.

## Reflection — where I redirected
  The plan came back slightly wrong on the first try because Claude did not fully understand how to keep the code beginner friendly, and still acheive the related tasks without changing anything pre-existing. I changed task 2's brief slightly to be more specific, so that I wouldn't have the same issue as task 1. I would have changed the decomposition to be much more specific and in a heirarchical order so that Clause has a proper workflow to work through.